# API outline

GET /api/health
GET /api/verify/:id

Production additions: authenticated student CRUD, certificate issuance, revocation, audit retrieval, document upload and blockchain transaction status.
