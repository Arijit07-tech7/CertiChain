{
  "name": "certivault-fullstack",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev": "npm run dev:client",
    "dev:client": "npm --prefix client run dev",
    "dev:server": "npm --prefix server run dev",
    "build": "npm --prefix client run build",
    "start:server": "npm --prefix server run start"
  }
}
