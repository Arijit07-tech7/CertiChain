import React from 'react';import {ShieldCheck,LockKeyhole,Database,KeyRound,Activity,Network,AlertTriangle} from 'lucide-react';
const cards=[['Cryptographic fingerprint', 'SHA-256', 'Every issued credential receives a deterministic document fingerprint.',LockKeyhole],['Issuer registry','Trusted issuer', 'Institutional issuer identity is separated from public certificate data.',ShieldCheck],['Revocation registry','Enabled','Credentials can be invalidated without altering the original record.',AlertTriangle],['Audit trail','Append-only design','Issue and revoke events are recorded for review.',Activity],['IPFS / document store','Ready for integration','Document storage can be pinned separately from identity records.',Database],['Blockchain anchor','Deployment ready','Solidity contract scaffold is included in the project.',Network],['Key management','Production requirement','Use server-side/HSM-backed issuer keys; never ship private keys to the browser.',KeyRound]];
export default function SecurityCenter(){return <><div className="page-head"><div><span className="eyebrow">DEFENSE LAYER</span><h1>Security Center</h1><p>Credential integrity controls designed around least exposure and verifiable records.</p></div></div><div className="security-banner"><ShieldCheck size={30}/><div><b>Workspace security posture</b><span>Frontend demo mode • production controls are scaffolded separately.</span></div><strong>MONITORED</strong></div><div className="security-grid">{cards.map(([a,b,c,I])=><div className="security-card" key={a}><div className="sec-icon"><I size={19}/></div><span>{a}</span><b>{b}</b><p>{c}</p></div>)}</div><div className="panel threat"><span className="eyebrow">THREAT MODEL</span><h3>What production deployment must protect</h3><div className="threat-grid"><div>Private issuer keys<small>Keep in HSM/KMS or secure server environment.</small></div><div>Admin sessions<small>Use server-side RBAC, MFA and short-lived sessions.</small></div><div>Public verification<small>Rate-limit, cache safely and monitor abuse.</small></div><div>Document integrity<small>Hash the canonical document and anchor its fingerprint.</small></div></div></div></>}
