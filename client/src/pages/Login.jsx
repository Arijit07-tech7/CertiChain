import React,{useState} from 'react';
import {Navigate,useLocation,useNavigate} from 'react-router-dom';
import {LockKeyhole,ShieldCheck,ArrowRight,Eye,EyeOff} from 'lucide-react';
import {isLoggedIn,login} from '../services/auth';
export default function Login(){const nav=useNavigate();const loc=useLocation();const [user,setUser]=useState('');const [pass,setPass]=useState('');const [show,setShow]=useState(false);const [error,setError]=useState('');if(isLoggedIn())return <Navigate to="/" replace/>;function submit(e){e.preventDefault();setError('');if(login(user.trim(),pass))nav(loc.state?.from?.pathname||'/');else setError('Invalid administrator credentials.')}return <div className="login-page"><div className="login-glow glow-one"/><div className="login-glow glow-two"/><div className="login-card"><div className="login-brand"><div className="brand-mark big">C</div><div><b>CertiVault</b><span>Credential Security Platform</span></div></div><div className="login-kicker"><ShieldCheck size={15}/> NIT • JIS GROUP • SECURE ISSUER</div><h1>Administrator<br/><em>access.</em></h1><p>Sign in to issue, view, verify and manage academic credentials.</p><form onSubmit={submit}><label>User ID<input autoFocus value={user} onChange={e=>setUser(e.target.value)} placeholder="admin"/></label><label>Password<div className="password-field"><input type={show?'text':'password'} value={pass} onChange={e=>setPass(e.target.value)} placeholder="••••••••••"/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff size={17}/>:<Eye size={17}/>}</button></div></label>{error&&<div className="login-error">{error}</div>}<button className="btn primary login-btn">Unlock issuer workspace <ArrowRight size={17}/></button></form><div className="login-footer"><LockKeyhole size={13}/> Admin-only workspace • Session protected</div></div></div>}
