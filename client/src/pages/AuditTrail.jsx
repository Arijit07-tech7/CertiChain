import React from 'react';import {History,Clock3,ShieldCheck} from 'lucide-react';import {loadState} from '../utils/storage';
export default function AuditTrail(){const a=loadState().audit;return <><div className="page-head"><div><span className="eyebrow">IMMUTABLE-STYLE EVENT LOG</span><h1>Audit Trail</h1><p>Review credential lifecycle events generated by this workspace.</p></div></div><div className="panel">{a.length===0?<div className="empty"><History size={34}/><b>No events yet</b><span>Issue or revoke a certificate to create the first event.</span></div>:<div className="timeline">{a.map(x=><div className="event" key={x.id}><div className="event-icon"><ShieldCheck size={16}/></div><div><b>{x.action}</b><span>{x.detail}</span></div><time><Clock3 size={13}/>{new Date(x.at).toLocaleString()}</time></div>)}</div>}</div></>}
