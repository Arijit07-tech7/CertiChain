import React,{useState} from 'react';import {UserPlus,GraduationCap,Mail,BookOpen,CalendarDays} from 'lucide-react';import {loadState,saveState} from '../utils/storage';
const blank={name:'',studentId:'',department:'',course:'',batch:'',email:'',graduationYear:''};
export default function Students(){const [state,setState]=useState(loadState());const [form,setForm]=useState(blank);const [done,setDone]=useState(false);function add(e){e.preventDefault();if(!form.name||!form.studentId||!form.department||!form.course)return;const student={...form,id:crypto.randomUUID(),createdAt:new Date().toISOString()};const next={...state,students:[...state.students,student]};saveState(next);setState(next);setForm(blank);setDone(true);setTimeout(()=>setDone(false),1800)}return <><div className="page-head"><div><span className="eyebrow">STUDENT REGISTRY</span><h1>Students</h1><p>Add verified academic identities before issuing credentials.</p></div></div><div className="grid-two students-layout"><form className="panel form-panel" onSubmit={add}><div className="panel-head"><div><span className="eyebrow">NEW RECORD</span><h3>Student details</h3></div><UserPlus size={20}/></div><label>Student name<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} placeholder="e.g. Arijit Gupta"/></label><label>Student ID / Roll<input value={form.studentId} onChange={e=>setForm({...form,studentId:e.target.value})} placeholder="e.g. NIT-IT-001"/></label><div className="form-grid"><label>Department<input value={form.department} onChange={e=>setForm({...form,department:e.target.value})} placeholder="Information Technology"/></label><label>Course<input value={form.course} onChange={e=>setForm({...form,course:e.target.value})} placeholder="B.Tech"/></label><label>Batch<input value={form.batch} onChange={e=>setForm({...form,batch:e.target.value})} placeholder="2022–2026"/></label><label>Graduation year<input value={form.graduationYear} onChange={e=>setForm({...form,graduationYear:e.target.value})} placeholder="2026"/></label></div><label>Email<input type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})} placeholder="student@example.com"/></label><button className="btn primary full"><GraduationCap size={17}/> Add student</button>{done&&<div className="success">✓ Student securely added.</div>}</form><div className="panel"><div className="panel-head"><div><span className="eyebrow">REGISTRY</span><h3>{state.students.length} student{state.students.length!==1?'s':''}</h3></div></div>{state.students.length===0?<div className="empty"><GraduationCap size={34}/><b>No students yet</b><span>Fresh workspace — add the first learner.</span></div>:<div className="list">{state.students.map(s=><div className="student-row" key={s.id}><div className="person">{s.name.split(' ').map(x=>x[0]).join('').slice(0,2).toUpperCase()}</div><div><b>{s.name}</b><span>{s.studentId} • {s.department}</span></div><small>{s.course}</small></div>)}</div>}</div></div></>}
