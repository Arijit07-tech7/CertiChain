import React,{useState} from 'react';import {useSearchParams} from 'react-router-dom';import {ScanLine,ShieldCheck,AlertTriangle,Search,LockKeyhole} from 'lucide-react';import {loadState} from '../utils/storage';
export default function Verify(){const [params]=useSearchParams();const [q,setQ]=useState(params.get('id')||'');const [result,setResult]=useState(null);function verify(){const s=loadState();const c=s.certificates.find(x=>x.id.toLowerCase()===q.trim().toLowerCase());setResult(c||'NOT_FOUND')}return <><div className="page-head"><div><span className="eyebrow">PUBLIC VERIFICATION</span><h1>Verify a credential</h1><p>Check the issuer record without exposing internal security material.</p></div></div><div className="verify-box panel"><div className="verify-icon"><ScanLine size={28}/></div><h2>Is this certificate authentic?</h2><p>Enter the certificate reference exactly as printed on the document.</p><div className="verify-input"><Search size={18}/><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&verify()} placeholder="NIT-2026-XXXXXXXXXX"/><button className="btn primary" onClick={verify}>Verify</button></div><div className="verify-note"><LockKeyhole size={14}/> Technical fingerprint is never displayed to the public.</div>{result&&<div className={result==='NOT_FOUND'?'result invalid':result.status==='REVOKED'?'result revoked':'result valid'}>{result==='NOT_FOUND'?<><AlertTriangle/><div><b>NOT FOUND</b><span>No issuer record matched this certificate ID.</span></div></>:result.status==='REVOKED'?<><AlertTriangle/><div><b>REVOKED</b><span>This credential has been revoked by the issuer.</span></div></>:<><ShieldCheck/><div><b>VALID / VERIFIED</b><span>Issuer record matched. {result.studentName} • {result.course} • {result.department}</span></div></>}</div>}</div></>}
