import React,{useState} from 'react';import {MessageCircle,X,Sparkles} from 'lucide-react';
export default function CricketBot(){const [open,setOpen]=useState(false);const messages=['Well played! Your issuer workspace is secure.','Six runs for clean certificates! 🏏','Need to verify? Hit the Verify section.','Keep certificates safe, verify every time.'];const [i,setI]=useState(0);function toggle(){setOpen(v=>!v);if(!open)setI(x=>(x+1)%messages.length)}return <div className="cricket-wrap">{open&&<div className="bot-card"><div className="bot-head"><div className="bot-avatar"><span className="mascot-mini">🏏</span></div><div><b>Cricket Desk</b><span>Animated credential assistant</span></div><button onClick={()=>setOpen(false)}><X size={15}/></button></div><div className="bot-msg"><Sparkles size={15}/> {messages[i]}</div><div className="bot-msg">Tap me anytime for another little match-day message.</div></div>}<button className="bot-fab" onClick={toggle} aria-label="Open cricket assistant"><span className="cricket-svg">🏏</span><MessageCircle size={17}/></button></div>}
